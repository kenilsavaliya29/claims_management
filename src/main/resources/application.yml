spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DB}?ssl-mode=REQUIRED
    username: ${MYSQL_USER}
    password: ${MYSQL_PASS}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: ${PORT}

jwt:
  secret: ${JWT_SECRET}